# ALL Detection Project — Python dependencies
# Pinned for Google Colab (Python 3.12) + numpy<2.0 ABI compatibility
# Install: pip install -r requirements.txt
# GPU note: for CUDA support replace the torch lines with:
#   pip install torch==2.3.1+cu121 torchvision==0.18.1+cu121 --extra-index-url https://download.pytorch.org/whl/cu121

# Numerical base — pinned to 1.26.x to avoid numpy 2.x ABI mismatch
numpy==1.26.4

# Deep learning — torch+torchvision must be updated together; Colab CUDA 12.x compatible
torch==2.3.1
torchvision==0.18.1

# Scientific computing — compiled against numpy 1.x (released before numpy 2.0, June 2024)
scipy==1.13.1

# Classical ML & image processing — all wheels predate numpy 2.0 release
scikit-learn==1.5.0
scikit-image==0.23.0
opencv-python==4.9.0.80

# Data
pandas==2.2.2

# Visualisation
matplotlib==3.9.0
seaborn==0.13.2

# Configuration & utilities
pyyaml==6.0.1
tqdm==4.66.4

# Dataset download
kaggle==1.6.14
